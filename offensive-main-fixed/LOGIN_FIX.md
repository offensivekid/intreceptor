# 🔥 РЕШЕНИЕ ПРОБЛЕМЫ С ЛОГИНОМ

## Проблема
Логин `offensivekid` не работает на Railway, хотя в `.env` файле указан.

## Причина
База данных на Railway уже была создана раньше с ДРУГИМ админом. Новые данные из `.env` не применяются к существующей базе.

## ✅ РЕШЕНИЯ (выбери одно)

---

## 🎯 Решение 1: Узнать текущий логин админа (САМОЕ ПРОСТОЕ)

Открой логи Railway и найди строки при первом запуске:
```
🔐 ADMIN CREDENTIALS (SAVE THESE!)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   Username: XXXXX
   Password: XXXXX
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

Используй эти данные для входа!

### Как посмотреть логи на Railway:
1. Открой свой проект на Railway
2. Перейди во вкладку **Deployments**
3. Выбери **самый первый** (самый старый) деплоймент
4. Открой **View Logs**
5. Найди строки с `ADMIN CREDENTIALS`

---

## 🎯 Решение 2: Проверить какой админ в базе

Открой в браузере:
```
https://offensive-production.up.railway.app/api/admin/check
```

Увидишь что-то типа:
```json
{
  "exists": true,
  "username": "admin",
  "email": "admin@offensive-forum.local",
  "isAdmin": true
}
```

Это и есть текущий админ! Используй этот `username` для входа.
Пароль ищи в логах первого деплоя (Решение 1).

---

## 🎯 Решение 3: Локальный сброс пароля (если есть доступ к базе)

Если можешь подключиться к контейнеру Railway или скачать базу:

```bash
# Запусти скрипт сброса пароля
node reset-admin-password.js
```

Следуй инструкциям на экране.

---

## 🎯 Решение 4: Удалить базу и пересоздать (РАДИКАЛЬНОЕ)

⚠️ **ВНИМАНИЕ: Это удалит ВСЕ данные!**

1. На Railway найди Volume где хранится база
2. Удали файл `database.sqlite` и `sessions.sqlite`
3. Перезапусти приложение
4. При старте создастся новая база с данными из `.env`
5. Админ будет: `offensivekid` с паролем `LUAKU-FURRY-LOVER-1L34`

---

## 📋 Быстрая диагностика

### 1. Проверь какой админ в базе:
```bash
curl https://offensive-production.up.railway.app/api/admin/check
```

### 2. Попробуй стандартные данные:
- Username: `admin` (если не менял)
- Password: *смотри в логах первого деплоя*

---

## 🔑 Переменные окружения в Railway

Убедись что в Railway Settings → Variables есть:

```env
ADMIN_USERNAME=offensivekid
ADMIN_PASSWORD=LUAKU-FURRY-LOVER-1L34
ADMIN_EMAIL=offensivekid@outlook.com
```

⚠️ **НО ПОМНИ:** Эти переменные работают только при СОЗДАНИИ новой базы!

---

## ❓ Часто задаваемые вопросы

**Q: Почему данные из .env не работают?**  
A: База уже создана раньше. .env работает только при первом создании таблиц.

**Q: Можно ли изменить админа без потери данных?**  
A: Да! Используй Решение 3 (локальный скрипт) или попроси того кто имеет доступ к базе выполнить SQL: `UPDATE users SET password_hash = '<новый_хеш>' WHERE username = 'admin'`

**Q: Где посмотреть оригинальный пароль?**  
A: В логах самого первого деплоя на Railway.

---

## 🆘 Если ничего не помогло

Напиши мне результат:
```bash
curl https://offensive-production.up.railway.app/api/admin/check
```

И я помогу дальше!
